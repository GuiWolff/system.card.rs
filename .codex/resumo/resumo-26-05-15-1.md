# Contexto
Você é um desenvolvedor Senior em Dart / Flutter.
Leia a análise desta tarefa antes de executar qualquer alteração.
Esta tarefa foi dividida em 4 slices.

## Análise da tarefa
- `docs/codex/resumo/resumo-26-05-15-1-analise.md`

## Objetivo geral
- Implementar, em etapas pequenas, o bloco de resumo financeiro da tela de recibo da System Card - RS.
- O bloco deve usar `lib/resources/resumo.png` como referência visual, ficar abaixo do widget de recibo/produtos na coluna da `ReciboPage` e exibir `Total do Pedido`, `Valor Entrada` e `Valor a pagar na Entrega`.
- O bloco final deve ser composto por widgets Flutter, com cálculo real e estado testável, não por uma imagem única.

## Arquivos principais envolvidos
- Existentes:
  - `AGENTS.md`
  - `pubspec.yaml`
  - `lib/main.dart`
  - `test/widget_test.dart`
  - `lib/resources/tema.jpeg`
  - `lib/resources/recibo.png`
  - `lib/resources/resumo.png`
  - `lib/features/recibo/presentation/pages/recibo_page-contrato.md`
- Esperados ou prováveis durante a execução:
  - `lib/features/recibo/presentation/pages/recibo_page.dart`
  - `lib/features/recibo/presentation/widgets/resumo_pedido.dart`
  - `lib/features/recibo/presentation/widgets/recibo_form.dart`
  - `lib/features/recibo/presentation/widgets/produtos_servicos.dart`
  - `lib/features/recibo/presentation/viewmodels/recibo_page_view_model.dart`
  - `lib/features/recibo/domain/models/item_recibo.dart`
  - `lib/features/recibo/domain/models/resumo_pedido.dart`
  - `test/features/recibo/presentation/widgets/resumo_pedido_test.dart`
  - `test/features/recibo/presentation/viewmodels/recibo_page_view_model_test.dart`

## Contratos de tela
- Contrato relacionado:
  - `lib/features/recibo/presentation/pages/recibo_page-contrato.md`
- Contratos existentes que devem ser lidos antes da alteração:
  - `lib/features/recibo/presentation/pages/recibo_page-contrato.md`
- Contratos que cada slice deve criar, atualizar ou revisar:
  - Slice 1: revisar o contrato para refletir os dados e regras de cálculo do resumo.
  - Slice 2: atualizar o contrato com o estado exposto pelo ViewModel ou controller.
  - Slice 3: atualizar o contrato com o layout do `ResumoPedido` abaixo do widget de recibo.
  - Slice 4: revisar o contrato após testes, validações e ajustes responsivos finais.

## Slices da tarefa

### Slice 1/4 - Modelo e cálculo do resumo
Arquivo: `docs/codex/resumo/resumo-26-05-15-1-parte_1.md`
Resumo esperado: `docs/codex/resumo/resumo-26-05-15-1-parte_1-resumo.md`

Atividades:
1. Identificar a estrutura real dos itens/produtos do recibo já existente ou planejada no código.
2. Criar ou ajustar modelos para representar itens do recibo e resumo financeiro.
3. Centralizar o cálculo de `total do pedido`, `valor entrada` e `valor a pagar na entrega`.
4. Adicionar testes de unidade para soma, entrada vazia, entrada zero, entrada válida e entrada maior que o total.
5. Revisar o contrato da `ReciboPage` com os dados necessários do resumo.

Validações:
- `flutter analyze`
- `flutter test test/features/recibo/presentation/viewmodels/recibo_page_view_model_test.dart`
- `flutter test`

### Slice 2/4 - Estado da tela e atualização reativa
Arquivo: `docs/codex/resumo/resumo-26-05-15-1-parte_2.md`
Resumo esperado: `docs/codex/resumo/resumo-26-05-15-1-parte_2-resumo.md`

Atividades:
1. Expor o resumo financeiro no `ReciboPageViewModel` ou controller equivalente.
2. Garantir que mudanças em produtos, quantidades, valores unitários e entrada atualizem o resumo.
3. Definir a regra de validação para `Valor Entrada`, sem colocar regra de negócio no widget visual.
4. Adicionar testes do ViewModel para recálculo e formatação monetária.
5. Atualizar o contrato da `ReciboPage` com estados e regras de interação do resumo.

Validações:
- `flutter analyze`
- `flutter test test/features/recibo/presentation/viewmodels/recibo_page_view_model_test.dart`

### Slice 3/4 - Widget visual abaixo do recibo
Arquivo: `docs/codex/resumo/resumo-26-05-15-1-parte_3.md`
Resumo esperado: `docs/codex/resumo/resumo-26-05-15-1-parte_3-resumo.md`

Atividades:
1. Criar `ResumoPedido` como widget composto e testável.
2. Integrar `ResumoPedido` abaixo do widget de recibo/produtos dentro da coluna da `ReciboPage`.
3. Seguir `lib/resources/resumo.png` como referência visual para título, três campos, destaque verde no saldo e espaçamentos.
4. Garantir que o layout não use `resumo.png` como imagem final única.
5. Adicionar testes de widget para textos, valores e ausência de overflow em larguras representativas.
6. Atualizar o contrato da `ReciboPage` com o estado visual do resumo.

Validações:
- `flutter analyze`
- `flutter test test/features/recibo/presentation/widgets/resumo_pedido_test.dart`

### Slice 4/4 - Integração, contrato e fechamento
Arquivo: `docs/codex/resumo/resumo-26-05-15-1-parte_4.md`
Resumo esperado: `docs/codex/resumo/resumo-26-05-15-1-parte_4-resumo.md`

Atividades:
1. Revisar a integração final do resumo com a `ReciboPage`.
2. Garantir consistência visual com `tema.jpeg`, `recibo.png` e `resumo.png`.
3. Garantir que os testes do template antigo não permaneçam como contrato falso do app.
4. Rodar validações finais.
5. Revisar e atualizar `recibo_page-contrato.md`.
6. Registrar pendências reais no resumo do slice.

Validações:
- `flutter analyze`
- `flutter test`

## Regras gerais
- Executar apenas um slice por vez.
- Nunca executar slices em paralelo.
- Nunca avançar para o próximo slice sem o resumo do slice atual.
- Se um resumo de slice já existir e estiver válido, não repetir esse slice.
- Cada slice deve considerar o estado atualizado do código produzido pelo slice anterior.
- Cada slice que alterar UI deve criar ou atualizar o respectivo `[nome-da-tela]-contrato.md`.
- Preservar alterações existentes no worktree.
- Não fazer commit automaticamente.
- Seguir `AGENTS.md`, mantendo português pt-BR, UTF-8 e arquitetura vertical feature-first.
- Não assumir que arquivos citados em documentos antigos existem no worktree atual. Verificar antes de importar ou depender deles.
- Se `ReciboPage` ou o widget de recibo/produtos ainda não existirem no momento da execução, não duplicar uma implementação paralela: integrar ao caminho real existente ou registrar a dependência de execução dos slices anteriores.

## Resultado esperado
- Ao final dos 4 slices, a tela de recibo deve exibir um bloco de resumo financeiro abaixo do widget de recibo/produtos.
- O resumo deve calcular e renderizar `Total do Pedido`, `Valor Entrada` e `Valor a pagar na Entrega` com valores monetários em pt-BR.
- A implementação deve ter estado testável, UI responsiva, contrato de tela atualizado e validações registradas nos resumos dos slices.
